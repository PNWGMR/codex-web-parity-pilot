# Web Smoke Artifacts

Playwright writes first-screen proof images here:

- `first-screen.png` for the desktop console viewport
- `mobile-first-screen.png` for the mobile console viewport

Run `npx playwright test` to refresh the screenshots, then run `nsctl web smoke` to validate them and write `web-smoke.json`.
